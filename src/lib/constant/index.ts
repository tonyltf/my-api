
export enum GrantType {
  refresh_token = 'refresh_token',
  password = 'password',
};

export enum HTTP_STATUS {
  OK = 200,
  CREATED = 201,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  SERVER_ERROR = 500,
};
